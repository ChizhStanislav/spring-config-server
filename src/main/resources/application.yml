server:
  port: 8084

spring:
  application:
    name: spring-cloud-config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:/
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
